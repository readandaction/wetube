mixin videoBlock(video={})
    .videoBlock
        a(href=routes.videoDetail(video.id))
            video.videoBlock__thumbnail(src=video.videoFile, control=true) 
            h4.videoBlock__title=video.title
            h6.videoVBlock__views=video.views