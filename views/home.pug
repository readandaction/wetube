extends layouts/main
include mixins/videoBlock

block content
    .video
        each item in videos
            +videoBlock({
                id: item.id,
                title: item.title,
                description: item.description,
                videoFile:item.videoFile
            })